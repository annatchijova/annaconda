# Service (Cloud Run) dependencies. The deterministic forensic CORE is
# stdlib-only by design; these are the web + agent layer on top of it.
fastapi==0.136.3
uvicorn[standard]==0.47.0
sse-starlette==3.4.4
google-adk==2.1.0
google-genai==1.75.0
google-cloud-firestore==2.28.1
# Reasoning-chain tracing to Cloud Trace (optional; degrades to no-op)
opentelemetry-sdk==1.41.1
opentelemetry-exporter-gcp-trace==1.14.0
